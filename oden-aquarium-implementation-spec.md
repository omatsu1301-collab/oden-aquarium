# おでんアクアリウム — 初期実装指示書(Cursor向け)

Tech Lead: 人間(依頼者) / 実装者: Cursor
本書はレビュー済みの設計を、実装者が推測を挟まずに実行できる形にまとめたものです。

---

## 1. Objective

「おでんアクアリウム」の最初の実ユーザー提供可能な形を作る。具体的には以下を実現する。

- React + Viteの新規プロジェクトを立ち上げ、公開URLでスマートフォンから閲覧できる状態にする
- キャラクター1体(大根)を画面に表示し、常時ふわふわと浮遊するアニメーションをつける
- 「時間経過で出汁が染み込んでいく」状態を、アプリを閉じて再度開いても正しく反映される形で実装する

## 2. Background

このアプリの核となる体験は「何もしなくても時間とともに変化している」こと。バックグラウンドで常にロジックを回し続ける実装は複雑さと不具合の温床になるため、「基準値+基準時刻」から現在値を都度計算する設計を採用することが既に決定している。今回はその設計の最小実装版を、実際にユーザーが触れる形で最速で成立させることが目的。

## 3. Current State

- コードベースは存在しない(ゼロからのプロジェクト立ち上げ)
- 既存の依頼者アセットとして、キャラクターデザインの参考イメージ(おでん種が擬人化された水族館のイラスト)が1枚ある。これは実装対象そのものではなく、キャラクター造形の参考資料
- 依頼者は過去にReact + Vite + GitHub Pagesでの開発・公開経験がある(前提知識としてこの構成を継続)

## 4. Target State

実装完了時、以下が成立していること。

- 公開URLにスマートフォンからアクセスすると、大根キャラクターが1体表示され、常時ゆらゆらと浮遊している
- ページを閉じて数時間後に再度開くと、「出汁の染み込み具合(dashiLevel)」が経過時間に応じて正しく変化した値になっている(画面上に数値かゲージとして表示)
- ブラウザのlocalStorageを消去した状態、または壊れたデータが入っている状態でアクセスしても、アプリがクラッシュせず初期状態から開始できる
- 開発者が経過時間を疑似的に進めて動作確認できる仕組みがある(本番ユーザーには見えない)

## 5. Invariants(今回の変更後も絶対に維持するもの)

- **[FIXED]** 状態計算ロジック(derive)はReactから独立した純粋関数として存在し、DOM操作・Reactフックに依存しない
- **[FIXED]** 永続化するのは「基準値(anchorValue)+基準時刻(anchorTime)」のみ。「現在の表示値」自体をlocalStorageに保存しない
- **[FIXED]** 表示用のdashiLevelはuseStateに保持して定期的に書き換える対象にしない。レンダー時(および明示的なイベント発生時)にderiveを呼んで都度算出する
- **[FIXED]** データ構造は複数キャラクターを前提にした形にする(`characters: { [id]: {...} }`)。現時点で1体しか存在しなくても、この形を崩さない

## 6. Non-Goals(今回やらないこと)

- タップ操作やインタラクション(反応・出汁追加ボタンなど)の実装
- 大根以外のキャラクター追加
- 図鑑・レア出現・収集要素
- ネイティブアプリ化、ホーム画面ウィジェット
- 不正な時刻操作への対策(時計を戻したら進行を止める、程度の最低限のみ扱う。詳細な検知ロジックは今回スコープ外)
- 状態データのマイグレーション機構(スキーマバージョンが将来変わった場合の移行処理)

## 7. Architecture Decision

- **[FIXED]** フレームワークはReact + Vite。新規の状態管理ライブラリ(Redux, Zustand等)、アニメーションライブラリ(Framer Motion, GSAP等)は導入しない。useState / Context / CSSアニメーション(またはrequestAnimationFrame)で今回の要件は満たせる規模と判断する
- **[FIXED]** 状態計算(derive)ロジックは、Reactコンポーネントとは別ファイル・別モジュールに分離する(「純粋ロジック層」と「表示層」を分ける)
- **[FLEXIBLE]** 浮遊アニメーションの実装手段(CSS @keyframes か、requestAnimationFrameでのJS制御か)はCursorの判断でよい。ただし新規ライブラリは追加しない
- **[FLEXIBLE]** ファイル配置・命名の細部はCursorの判断でよい(セクション9は「責務の分離」を示すものであり、ファイル名を厳密に指定するものではない)

理由:現時点でコードベースがなく、要件も「1体を表示して浮遊させ、時間経過を計算する」という比較的小さいスコープのため、状態管理ライブラリやアニメーションライブラリを導入する必要性がない。将来キャラクターが増えたり演出が複雑化した時点で再検討すればよく、今の時点で汎用性の高い構成にする理由はない。

## 8. Data / State Flow

```
[localStorage]
     │ 読み込み(起動時)
     ▼
[SaveData] ──(存在しない/壊れている場合)──▶ [初期状態を生成]
     │
     ▼
[characters.daikon.anchorValue / anchorTimeMs]
     │
     │  derive(character, nowMs) ※純粋関数、副作用なし
     ▼
[表示用 dashiLevel] ──▶ [Reactコンポーネントが都度算出して表示]
```

- **[FIXED]** deriveの呼び出しタイミングは「初回マウント時」「デバッグ操作(時間送り)発生時」の2つのみ。常時カウントダウンするintervalタイマーは実装しない
- **[FIXED]** 保存(localStorageへの書き込み)は、状態(anchorValueやanchorTimeMs)が実際に変化した操作の直後にのみ行う

## 9. Files / Responsibilities

**注記:現時点でコードベースが存在しないため、以下は「新規作成する構成の提案」であり、既存ファイルの断定ではない。具体的なファイル名・配置はCursorの判断で調整してよい([FLEXIBLE])。ただし責務の分離(ロジック層とUI層を別ファイルにする)は [FIXED]。**

- **状態計算ロジック(純粋関数)**:dashiLevelの算出、初期状態の生成、保存データの妥当性検証を担当。Reactに依存しない
- **永続化処理**:localStorageへの読み書き、JSON parseの例外処理、schemaVersionチェックを担当
- **UIコンポーネント(キャラクター表示)**:大根キャラクターの見た目・浮遊アニメーションの描画を担当。状態計算そのものは行わない
- **アセット**:キャラクター画像(透過PNG)

## 10. Implementation Order

### Step 1: プロジェクト初期化と公開
- **目的**:公開URLを1つ確保する
- **変更内容**:Vite + Reactの新規プロジェクトを作成し、画面に仮テキストを表示。GitHub PagesまたはVercelにデプロイ
- **依存する前工程**:なし
- **確認方法**:スマートフォンの実機ブラウザから公開URLにアクセスし、表示を確認する

### Step 2: 状態計算ロジックの実装
- **目的**:時間経過から現在のdashiLevelを算出できるようにする
- **変更内容**:`characters.daikon.anchorValue / anchorTimeMs` を受け取り、任意の時刻における現在値を返す純粋関数を実装。減少速度は名前付き定数として1箇所に定義する(**[FLEXIBLE]** 具体的な数値はCursorが仮決めしてよいが、コード中に直書きせず定数化すること)
- **依存する前工程**:Step 1
- **確認方法**:関数に任意の未来時刻を渡すユニットテスト(または簡易スクリプト)で、意図通りに値が減っていることを確認

### Step 3: 永続化処理の実装
- **目的**:状態をlocalStorageに保存・復元できるようにする
- **変更内容**:保存/読み込み処理を実装。読み込み時はtry-catchで例外を捕捉し、JSON parse失敗時・schemaVersion不一致時は初期状態にフォールバックする
- **依存する前工程**:Step 2
- **確認方法**:ブラウザの開発者ツールでlocalStorageの値を意図的に壊した状態でリロードし、クラッシュせず初期状態で起動することを確認

### Step 4: キャラクター表示と浮遊アニメーション
- **目的**:大根キャラクターを画面に表示し、Cozyな浮遊感を出す
- **変更内容**:透過PNG画像を配置し、上下移動・わずかな回転・スケール変化を組み合わせたループアニメーションを実装
- **依存する前工程**:Step 1
- **確認方法**:実機で見て「ゆらゆら動いている」と視覚的に確認できること

### Step 5: 状態とUIの結線
- **目的**:算出したdashiLevelを画面に反映する
- **変更内容**:マウント時にStep 3の読み込み処理→Step 2のderive処理を実行し、結果を画面上に数値またはゲージとして表示する
- **依存する前工程**:Step 2, 3, 4
- **確認方法**:localStorageの値を手動で書き換え(anchorTimeMsを過去にずらす)、リロード後に表示値が正しく変化していることを確認

### Step 6: デバッグ用の時間送り機能
- **目的**:実際に数時間〜数日待たずに動作検証できるようにする
- **変更内容**:URLクエリパラメータ(例:`?debug=1`)が付いている場合のみ、「anchorTimeMsを指定時間分過去にずらして再保存する」ボタンまたは入力欄を表示する。本番の通常URLでは表示しない
- **依存する前工程**:Step 2, 3, 5
- **確認方法**:`?debug=1`付きURLでボタン操作し、dashiLevelが変化・保存・リロード後も反映されていることを確認

## 11. Edge Cases

- localStorageが完全に空(初回起動) → 初期状態(anchorValue満タン、anchorTimeMs=起動時刻)を生成して保存
- localStorageの値がJSONとして壊れている → 例外を捕捉し、初期状態にフォールバック(エラーを画面に表示せず、静かに復旧する)
- schemaVersionが想定と異なる → 現時点ではマイグレーションを行わず、初期状態にフォールバック(Non-Goalsの通り)
- 端末の時計が過去に戻された場合 → 経過時間がマイナスにならないよう、deriveの内部で経過時間を0未満にクランプする(dashiLevelが増加方向に誤算出されないようにする)
- dashiLevelの計算結果が0未満または100(上限)超になり得る計算 → 必ず範囲内にクランプする

## 12. Constraints

- **[FORBIDDEN]** 状態管理ライブラリ・アニメーションライブラリなど、新規の外部依存パッケージを本指示書に明記されている以外の理由で追加すること
- **[FORBIDDEN]** dashiLevelを一定間隔で自動更新し続けるタイマー処理(setInterval等でのバックグラウンド進行)を実装すること
- **[FORBIDDEN]** 現在の表示値そのものをlocalStorageに保存すること(anchorValue/anchorTimeMs以外の値を永続化の主とすること)
- **[FORBIDDEN]** 本タスクの範囲外にある機能(タップ反応、出汁追加UI、複数キャラクター表示、Widget対応など)を「ついでに」実装すること
- **[FORBIDDEN]** 目的外のリファクタリング(今回触る必要のない箇所のコードスタイル変更・再構成)
- 軽微な実装判断(変数名、コンポーネントの細かい分割方法、CSSの具体的な数値など)はCursorが合理的に判断してよい
- **本指示書の内容と矛盾する、あるいは判断がつかない仕様上の問題が実装中に見つかった場合は、Cursorが独自に解釈して進めず、その内容を報告すること**

## 13. Acceptance Criteria

- [ ] 公開URLにスマートフォンの実機ブラウザからアクセスできる
- [ ] 大根キャラクターが画面に表示され、常時浮遊アニメーションが動いている
- [ ] dashiLevelが画面上に表示されている
- [ ] タブを閉じて再度開いても、経過時間に応じてdashiLevelが正しく変化している
- [ ] localStorageを手動で破壊した状態でアクセスしてもクラッシュせず、初期状態で起動する
- [ ] `?debug=1`付きURLで時間送り操作ができ、その結果が保存・リロード後も反映される
- [ ] 状態計算ロジック(derive)がReactコンポーネントから分離されたファイルに存在する
- [ ] 新規の外部依存パッケージが、本指示書に明記された理由なく追加されていない

## 14. Verification

- 実機のスマートフォンから公開URLを開き、目視で浮遊アニメーションとdashiLevel表示を確認する
- ブラウザの開発者ツールでlocalStorageの中身を確認し、`anchorValue` / `anchorTimeMs` のみが保存されており、表示用の現在値そのものは保存されていないことを確認する
- localStorageの値を手動でランダムな壊れた文字列に書き換え、リロードしてクラッシュしないことを確認する
- `?debug=1`で6時間分進めるボタンを押し、dashiLevelが想定通り減少することを確認する。その後リロードし、値が保持されていることを確認する
- ブラウザのコンソールにエラーが出力されていないことを確認する

## 15. Completion Report

実装完了後、以下を報告すること。

- 実装したファイル一覧と、それぞれの責務(1行程度)
- 出汁の減少速度として採用した具体的な数値と、その定数がどこに定義されているか
- Acceptance Criteriaの各項目について、確認済みかどうか
- 本指示書との差異が生じた箇所があれば、その内容と理由
- 実装中に判断がつかず報告が必要と判断した仕様上の矛盾があれば、その内容(なければ「なし」と明記)
- 公開URL
